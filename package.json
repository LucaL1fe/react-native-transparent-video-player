{
  "name": "react-native-transparent-video-skia-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "web"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspace web"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  }
}
